import { AfterViewInit, ElementRef, OnDestroy } from '@angular/core';
import { TranslateService } from './translate.service';
import { Observable } from 'rxjs';
export declare class TranslateDirective implements AfterViewInit, OnDestroy {
    element: ElementRef;
    translateService: TranslateService;
    keyPath: string;
    translateParams: any;
    translationLoaded$: Observable<boolean>;
    private unsubscribe;
    params: any;
    constructor(element: ElementRef, translateService: TranslateService);
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    private registerKeyChecker(keyPath);
    private runOneCheck(keyPath, params);
    private doCheck(isLoaded, keyPath, params);
}
